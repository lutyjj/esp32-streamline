# Two-slot OTA layout sized to fit 4 MB flash, the common ESP32 module size. The
# bootloader writes each update to the inactive app slot and flips `otadata`, so
# a failed image rolls back to the slot that last booted cleanly. App slots are
# 64 KB-aligned as the ESP32 bootloader requires. `make firmware-artifacts`
# fails when the application image exceeds a slot minus the declared margin, so
# the fit is enforced, not assumed. An 8 MB board runs this layout too, leaving
# its upper half unused.
# The coredump partition fills the alignment gap below the first 64 KB-aligned
# app slot, so it adds crash capture without moving any other partition. On a
# device flashed before it existed, the firmware reports crash capture as
# unavailable until a USB reflash lands the current table.
# Name,     Type, SubType,  Offset,   Size
nvs,        data, nvs,      0x9000,   0x6000
otadata,    data, ota,      0xf000,   0x2000
phy_init,   data, phy,      0x11000,  0x1000
coredump,   data, coredump, 0x12000,  0xE000
ota_0,      app,  ota_0,    0x20000,  0x1F0000
ota_1,      app,  ota_1,    0x210000, 0x1F0000
