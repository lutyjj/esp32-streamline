# Two-slot OTA layout for 8 MB flash. The bootloader writes each update to the
# inactive app slot and flips `otadata`, so a failed image rolls back to the
# slot that last booted cleanly. App slots are 64 KB-aligned as the ESP32
# bootloader requires; 3 MB each leaves ample headroom over the ~1.2 MB image.
# Name,     Type, SubType, Offset,   Size
nvs,        data, nvs,     0x9000,   0x6000
otadata,    data, ota,     0xf000,   0x2000
phy_init,   data, phy,     0x11000,  0x1000
ota_0,      app,  ota_0,   0x20000,  0x300000
ota_1,      app,  ota_1,   0x320000, 0x300000
